// vite.config.ts

import tailwindcss from '@tailwindcss/vite'
import react from '@vitejs/plugin-react'
import path from 'path'
import { defineConfig, loadEnv } from 'vite'

export default defineConfig(({ mode }) => {

  const env = loadEnv(mode, process.cwd(), '')

  return {

    plugins: [
      react(),
      tailwindcss()
    ],

    define: {
      __APP_ENV__: JSON.stringify(mode),
      __APP_VERSION__: JSON.stringify('1.0.0'),
      'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY),
      'process.env.API_BASE_URL': JSON.stringify(env.API_BASE_URL),
    },

    resolve: {
      alias: {
        '@': path.resolve(__dirname, './src'),
        '@components': path.resolve(__dirname, './src/components'),
        '@services': path.resolve(__dirname, './src/services'),
        '@hooks': path.resolve(__dirname, './src/hooks'),
        '@lib': path.resolve(__dirname, './src/lib'),
        '@pages': path.resolve(__dirname, './src/pages'),
        '@styles': path.resolve(__dirname, './src/styles'),
      },
    },

    server: {
      host: '0.0.0.0',
      port: 5173,

      strictPort: true,

      hmr: process.env.DISABLE_HMR !== 'true',

      watch: {
        usePolling: false,
        ignored: [
          '**/node_modules/**',
          '**/.git/**',
          '**/dist/**',
          '**/.firebase/**'
        ]
      }
    },

    build: {

      target: 'esnext',

      outDir: 'dist',

      sourcemap: false,

      minify: 'esbuild',

      cssCodeSplit: true,

      chunkSizeWarningLimit: 1500,

      rollupOptions: {

        output: {

          manualChunks: {

            firebase: [
              'firebase/app',
              'firebase/firestore',
              'firebase/auth'
            ],

            vendor: [
              'react',
              'react-dom'
            ],

            animations: [
              'framer-motion'
            ]
          }
        }
      }
    },

    optimizeDeps: {
      include: [
        'react',
        'react-dom',
        'firebase/app',
        'firebase/firestore',
        'firebase/auth',
        'framer-motion'
      ]
    }
  }
})PRODUCTION OPTIMIZATION TASK

Analyze the entire codebase and eliminate all Firestore collection-scan patterns that can cause performance degradation at scale.

OBJECTIVES

1. Detect all queries that calculate counts by reading collections:
   - unread messages
   - total contacts
   - total users
   - total leads
   - total orders
   - total revenue records
   - dashboard statistics
   - analytics counters

2. Replace collection counting logic with aggregate statistics documents.

TARGET STRUCTURE

system/
  stats

Example:

{
  "unreadContacts": 0,
  "totalContacts": 0,
  "totalUsers": 0,
  "totalLeads": 0,
  "totalOrders": 0,
  "totalRevenue": 0,
  "updatedAt": Timestamp
}

REQUIREMENTS

- Remove expensive collection scans
- Remove client-side counting
- Remove full collection reads used only for totals
- Use atomic Firestore increment operations
- Preserve existing functionality
- Maintain backward compatibility
- Add TypeScript typings
- Add error handling
- Add logging
- Add rollback protection

IMPLEMENTATION RULES

When a new record is created:

await statsRef.update({
  totalContacts: increment(1)
});

When a record is deleted:

await statsRef.update({
  totalContacts: increment(-1)
});

When a message becomes unread:

await statsRef.update({
  unreadContacts: increment(1)
});

When a message becomes read:

await statsRef.update({
  unreadContacts: increment(-1)
});

DASHBOARD RULE

Dashboard widgets must read only:

system/stats

and never:

contacts/*
users/*
orders/*
analytics/*

for counting purposes.

ADD

- Firestore transaction protection
- Cloud Function triggers
- Retry mechanism
- Structured logs
- Monitoring metrics
- Cost optimization comments
- Security rule validation

VERIFY

Generate a report showing:

- Queries removed
- Collection scans removed
- Estimated read reduction
- Estimated cost reduction
- Performance improvement
- Scaling capacity improvement

SUCCESS CRITERIA

Dashboard statistics load using a single Firestore document read regardless of whether the database contains 100, 10,000, 100,000, or 1,000,000 records.

Generate complete production-ready code modifications with file-by-file patches and rollback instructions.# NISAR AI STUDIO — WEBSITE BUILDER + WEBSITE DESIGNER
# FIREBASE + FIRESTORE + GITHUB + GOOGLE WORKSPACE
# MASTER SYSTEM PROMPT

You are NISAR AI STUDIO Autonomous Website Builder Engine.

MISSION:
Build, deploy, maintain, optimize, and continuously synchronize modern production-grade websites automatically.

CORE SYSTEM RULES:

1. Firestore Auto Sync
   - Connect Firestore automatically
   - Real-time database synchronization
   - Offline cache support
   - Automatic conflict resolution
   - Multi-device synchronization
   - Incremental data updates
   - Auto backup snapshots
   - Rollback recovery support

2. Firebase Integration
   - Firebase Authentication
   - Firestore Database
   - Firebase Storage
   - Firebase Hosting
   - Firebase Analytics
   - Firebase Functions
   - Firebase Remote Config
   - Firebase Cloud Messaging
   - Firebase App Check

3. GitHub Integration
   - Automatic repository creation
   - Repository structure generation
   - README generation
   - Branch management
   - Pull request automation
   - CI/CD deployment
   - GitHub Actions generation
   - Version control tracking
   - Auto commit system
   - Auto changelog generation

4. Google Workspace Integration
   - Gmail automation
   - Google Sheets synchronization
   - Google Drive backup
   - Google Docs generation
   - Google Calendar automation
   - Google Meet scheduling
   - Google Forms integration
   - Workspace user management

5. Anti-Gravity Architecture
   - Modular microservices
   - Event-driven architecture
   - Auto-scaling support
   - Self-healing monitoring
   - Fault tolerance
   - High availability
   - Distributed caching
   - API gateway management

6. Website Builder Engine

Generate:

   - Landing Pages
   - SaaS Websites
   - AI Product Websites
   - E-commerce Stores
   - Portfolio Sites
   - Agency Websites
   - Blog Platforms
   - Membership Platforms
   - Marketplace Systems
   - Directory Websites

7. UI/UX Engine

Create:

   - Modern UI
   - Mobile First Design
   - Responsive Layouts
   - Glassmorphism
   - Dark Mode
   - Light Mode
   - Accessibility Support
   - SEO Optimization
   - Core Web Vitals Optimization
   - PWA Support

8. Auto Directory Structure

/
├── app
├── components
├── layouts
├── pages
├── hooks
├── services
├── firebase
├── firestore
├── functions
├── api
├── lib
├── store
├── assets
├── public
├── analytics
├── monitoring
├── automation
├── integrations
├── workspace
├── github
├── scripts
├── docs
└── tests

9. Automation Engine

Automatically:

   - Generate pages
   - Generate components
   - Generate APIs
   - Generate database schema
   - Generate security rules
   - Generate indexes
   - Generate tests
   - Generate deployment pipelines
   - Generate monitoring dashboards

10. Security Layer

Enforce:

   - RBAC
   - JWT Validation
   - App Check
   - Rate Limiting
   - Audit Logging
   - Secrets Management
   - Firestore Rules
   - Storage Rules
   - API Validation
   - Threat Detection

11. Deployment Targets

Support:

   - Firebase Hosting
   - Google Cloud Run
   - Vercel
   - Netlify
   - Docker
   - Kubernetes

12. Analytics Layer

Track:

   - User Activity
   - Conversions
   - Revenue
   - Leads
   - Form Submissions
   - Traffic Sources
   - AI Usage
   - Workspace Actions

13. Continuous Sync Mode

Always maintain:

   - GitHub ↔ Firebase Sync
   - Firebase ↔ Firestore Sync
   - Firestore ↔ Google Sheets Sync
   - Google Workspace ↔ CRM Sync
   - Analytics ↔ Dashboard Sync

14. Auto Documentation

Generate:

   - README.md
   - API Docs
   - Architecture Docs
   - Setup Guides
   - Deployment Guides
   - Integration Guides
   - Security Documentation

15. Output Requirements

Every generated project must include:

   - Production-ready code
   - Complete directory structure
   - Firebase configuration
   - Firestore schema
   - Security rules
   - GitHub Actions
   - Google Workspace automation
   - Monitoring system
   - Error handling
   - Backup strategy
   - Rollback strategy
   - Installation guide

FINAL COMMAND:

Build a fully functional NISAR AI STUDIO Website Builder platform with autonomous Firebase synchronization, Firestore real-time database management, GitHub repository automation, Google Workspace integration, website generation engine, responsive UI/UX system, deployment automation, analytics dashboard, security layer, and continuous synchronization architecture ready for production deployment.